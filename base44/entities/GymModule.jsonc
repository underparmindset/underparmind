{
  "name": "GymModule",
  "type": "object",
  "properties": {
    "title": {
      "type": "string",
      "description": "Module title"
    },
    "summary": {
      "type": "string",
      "description": "Short summary shown in the card"
    },
    "content": {
      "type": "string",
      "description": "Full instructional content (supports markdown)"
    },
    "category": {
      "type": "string",
      "enum": [
        "Mindset",
        "Routine",
        "Focus",
        "Resilience",
        "Confidence",
        "Course Management"
      ],
      "description": "Module category"
    },
    "week_label": {
      "type": "string",
      "description": "e.g. 'Week of Jun 16' \u2014 displayed as a badge"
    },
    "is_featured": {
      "type": "boolean",
      "default": false,
      "description": "Pin to top as this week's featured module"
    },
    "order": {
      "type": "number",
      "description": "Display order"
    },
    "video_url": {
      "type": "string",
      "description": "URL of an uploaded video or external video link (YouTube, Vimeo, etc.)"
    },
    "article_url": {
      "type": "string",
      "description": "Link to an external article"
    },
    "article_title": {
      "type": "string",
      "description": "Display title for the article link"
    },
    "social_links": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "platform": {
            "type": "string"
          },
          "url": {
            "type": "string"
          },
          "label": {
            "type": "string"
          }
        }
      },
      "description": "Social media links (Instagram, TikTok, YouTube, X, etc.)"
    }
  },
  "required": [
    "title",
    "summary",
    "content"
  ],
  "rls": {
    "create": {
      "user_condition": {
        "role": "admin"
      }
    },
    "read": {},
    "update": {
      "user_condition": {
        "role": "admin"
      }
    },
    "delete": {
      "user_condition": {
        "role": "admin"
      }
    }
  }
}