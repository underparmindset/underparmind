{
  "name": "Goal",
  "type": "object",
  "properties": {
    "goal_text": {
      "type": "string"
    },
    "measure": {
      "type": "string"
    },
    "horizon": {
      "type": "string",
      "enum": [
        "week",
        "month",
        "quarter"
      ]
    },
    "status": {
      "type": "string",
      "enum": [
        "open",
        "hit",
        "missed"
      ],
      "default": "open"
    },
    "resolved_date": {
      "type": "string",
      "format": "date"
    }
  },
  "required": [
    "goal_text",
    "horizon"
  ],
  "rls": {
    "create": true,
    "read": {
      "created_by_id": "{{user.id}}"
    },
    "update": {
      "created_by_id": "{{user.id}}"
    },
    "delete": {
      "created_by_id": "{{user.id}}"
    }
  }
}